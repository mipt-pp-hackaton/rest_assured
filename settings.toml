[app_settings]
host = "0.0.0.0"
port = 8000
use_testcontainers = "True"

[db_settings]
name = "test_db"
user = "user"
password = "password"
host = "postgres"
port = 5432

[scheduler]
default_interval_ms = 60000
http_timeout_seconds = 5.0
max_retries = 2
retry_delay_ms = 1000
